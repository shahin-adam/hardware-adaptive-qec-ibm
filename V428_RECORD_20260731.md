# V428 â€” confidence-bound validation gate

V428 uses the same nested 60% train / 20% validation / 20% untouched-test split as V427, but enables a specialist only when its paired validation bootstrap 95% lower bound is non-negative.

All six basis/round policies failed that strict validation criterion. V428 therefore retained V12 for every test shot:

- V12 test error: 46.6018519%
- V428 test error: 46.6018519%
- Gain: **0.0000 pp**
- Bootstrap CI: **[0.0000,0.0000] pp**
- No subgroup regressions.

Decision: **safe fallback / diagnostic, not an accuracy promotion**. This establishes a conservative gate that prevents V427-style subgroup regressions, at the cost of no gain on this dataset.

Wolffe replay on compute-000 using system Python 3.14/NumPy 2.4.6 completed successfully and matched the local policies, test metrics, and subgroup rows exactly.
