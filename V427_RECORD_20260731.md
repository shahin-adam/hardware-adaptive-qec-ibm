# V427 â€” nested train/validation/test safety gate

V427 selects basis/round specialist policies on the first 60% of every IBM Fez V29 slice, enables them only when a separate 20% validation split is non-regressing, and reports the final 20% untouched test split.

Results on the untouched test split (21,600 shots):

- V12/MWPM: 46.6018519%
- V427: 46.5000000%
- Gain: **+0.1018519 pp**
- Paired bootstrap 95% CI: **[-0.1851852,+0.3796296] pp**

One final subgroup regressed:

- basis 1, round 7: âˆ’0.2222222 pp

Decision: **diagnostic/not promoted**. Nested selection reduces leakage risk, but the final confidence interval crosses zero and the subgroup gate fails.

The same script was replayed on Wolffe compute-000 via Slurm `srun` using system Python 3.14/NumPy 2.4.6. The downloaded Wolffe JSON matched the local test metrics, policies, and all subgroup rows exactly.
